# 2026-04-08 TIL

## 오늘 이해한 것

- 예약 도메인도 작은 계산기와 검증기로 쪼개면 Java 기초 문법 연습 주제로 만들 수 있다.
- 단순한 기능이라도 입력 검증, enum 분리, 메서드 책임 분리가 있어야 이후 확장이 쉬워진다.
- 예약 가능 여부는 단순 `true/false`보다 이유를 함께 반환해야 이후 예외 처리와 API 응답으로 연결하기 좋다.

## 아직 헷갈리는 것

- 할인과 할증이 함께 있을 때 계산 순서를 어떻게 고정해야 가장 자연스러운지
- 문자열 시간 표현으로만 검증할지, 지금처럼 `LocalTime`으로 변환해서 다룰지에 대한 기준

## 내가 스스로 해결한 것

- `TimeSlotGenerator`에서 운영 시간 검증과 간격 검증을 먼저 한 뒤 슬롯을 생성하도록 흐름을 정리했다.
- 가격 계산 규칙을 기본 요금, 주말 할증, VIP 할인, 장기 이용 할인으로 나눠 메서드 책임을 분리했다.
- 예약 검증 결과를 문자열 대신 enum으로 바꿔 오타 위험을 줄였다.

## AI가 도와준 것

- Day 1 과제의 요구사항과 현재 코드의 차이를 빠르게 점검했다.
- 빠진 산출물인 `README`, `til`, 폴더 구조를 같이 맞추는 흐름을 정리했다.

## AI 답변 중 믿지 않고 직접 검증한 것

- `ReservationPriceCalculator`가 원래 요구사항과 다르게 현재 날짜로 주말을 판별하고 있던 점
- `ReservationValidator`가 사실상 미구현 상태였던 점
- Day 1 합격 기준에 README와 TIL이 포함되어 있다는 점

## 오늘 AI에게 물어본 것

- 프롬프트 기준으로 현재 소스가 Day 1에서 어디까지 진행된 상태인지
- 어떤 부분이 요구사항과 어긋나 있는지

## AI 답변 중 채택한 것

- enum 분리
- 검증 로직 보강
- 문서 산출물 추가

## AI 답변 중 폐기한 것

- Day 2 객체지향 리팩터링까지 한 번에 넘어가는 것

## 폐기 이유

- 지금은 Day 1 진행 중인 저장소이므로 범위를 넘기지 않고 현재 목표를 먼저 명확히 끝내는 편이 좋다고 판단했다.

## 내일 할 일

- 각 클래스에 다양한 입력값으로 테스트 케이스를 더 넣기
- Day 2 리팩터링 대상인 `Room`, `Reservation`, `ReservationService` 초안 잡기
